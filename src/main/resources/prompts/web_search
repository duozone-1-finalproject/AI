[검색 키워드]
<keywords_json>

[작업 지시]
- 위 JSON 배열의 각 키워드에 대해 DuckDuckGo MCP "search" 도구를 반드시 호출해 결과를 수집하세요.
- 쿼리는 키워드 원문을 그대로 사용합니다(확장/번역/필터 금지).
- 각 키워드당 상위 <per_keyword>개만 선별하고, 중복 URL은 1건만 남깁니다.
- 가능한 경우 게시/업데이트 날짜를 파싱하여 YYYY-MM-DD로 기록하고, 불명확하면 null.
- 도메인 기준으로 출처를 ["뉴스","블로그","논문","정부","기타"] 중 하나로 분류합니다.
- 최종 출력은 아래 JSON 배열 스키마만 반환합니다. 배열 길이는 (키워드 수 × <per_keyword>)여야 합니다.

[최종 출력 JSON 스키마]
[
  {
    "검색keyword": "<원본 키워드>",
    "title": "<제목>",
    "url": "<정규화된 URL>",
    "date": "<YYYY-MM-DD 또는 null>",
    "source": "<뉴스|블로그|논문|정부|기타>"
  }
]

[중요]
- 반드시 MCP "search" 도구 호출 결과만으로 작성하세요. 추측/환각 생성 금지.
- JSON 외 어느 텍스트도 출력하지 마세요.
generate the results only by calling the MCP "search" tool. Do not guess or hallucinate.
[사용자 쿼리]
{query}

[평가 대상 기사들(JSON 배열)]
{articles_json}
# 배열의 각 원소 예시:
# {"title":"...", "content":"..."}  // content는 크롤링된 본문(일부/전부). URL/날짜가 없으므로 텍스트 힌트만 사용.

[지시]
- 위 "시스템 원칙"과 "점수 기준"을 엄격히 따르라.
- 위 “시스템 프롬프트”의 공시기준 기반 topic_fit 루브릭을 엄격 적용.
- 각 기사를 독립적으로 평가하여 reviews 배열에 추가하라.
- 출처는 텍스트 내 힌트로만 판정.
- 출력은 반드시 단일 JSON으로만 하라. 추가 텍스트/설명/코드블록 금지.
